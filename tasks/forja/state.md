# Estado del loop /forja v2 (motor global ~/.claude · este repo)

> **Protocolo**: autónomo sin confirmaciones (seguir solo; parar SOLO con "para" → resumen).
> Generador≠evaluador (otro modelo). NUNCA auto-merge: PR único acumulado + lo difícil como
> rama+test+diff. Para por cuota → resume. Bitácora viva abajo (1 línea/turno con resultado).

## Gates detectadas (este repo)
- backend (desde `backend/`, venv `…/automatizacion-backend-GS4zGL1Q-py3.11`): `ruff check app/ --select E,W,F,I --ignore E501,E402,E701,E702,E731,W293` (bloqueante) · `pytest tests/ -q` (necesita Postgres) · `mypy app/agents app/services --strict` (señal).
- frontend (desde `frontend/`): `npx tsc --noEmit` · `npx eslint src/ --max-warnings 20` · `npm run test:ci`. ⚠️ worktree sin node_modules → enlazar antes de verificar frontend.
- GitNexus: indexado (300 flujos, master). Discovery POR FLUJO.

## Sweep v2: por flujo de ejecución + lente rotativa
`lente actual: 1 (correctitud/casos borde — atómico)`. Lentes: 1 correctitud·borde / 2 seguridad / 3 concurrencia·idempotencia / 4 errores·red / 5 test-gaps / 6 perf·recursos.
Flujos recorridos (cursor): _empezando_. Áreas fiscales (billing/aeat/reports) = alto riesgo: fix solo a PR, evaluador fiscal estricto, nunca cambiar resultados sin evidencia, línea roja (no falsificar justificante/CSV/PDF417).

## Bitácora del loop
| fecha/hora | flujo/área · lente | resultado |
|------------|--------------------|-----------|
| 2026-06-25 | (v2 arrancada) | reindex GitNexus ok; worktree fresco; primer barrido: emisión factura + VeriFactu |
| 2026-06-25 ~19:30 | emisión+VeriFactu · L1 correctitud/borde (GitNexus) | FINDER halló 4 (atómicos de cadena). TEST-FIRST **refutó la #1** (db.bind no es None → lock SÍ se adquiere; falso positivo) y dejó **3 tests-guard verdes → PR #52**. #2 TOCTOU, #3 ⚠️ anulación sin encadenar (legal), #4 redondeo → inbox. Próx: lente 2 (seguridad) u otro flujo. |
| 2026-06-25 ~20:22 | doc→dispatch→agente · L2 seguridad (GitNexus) | Flujo MAYORMENTE LIMPIO (propagación de tenant correcta; finder descartó 3 falsos positivos: IDOR, race ContextVar, retry Celery). **1 fix**: guard fail-safe en `_execute_orchestrator` (aborta si hint None Y ContextVar vacío, en vez de query sin aislamiento; commit c44f65a → **PR #52**, evaluador PASS — confirmó que los 2 tests ajustados NO se debilitaron, 160 verde). Pendiente baja: anotar `tenant_id` en `_dispatch_task`. Próx: lente 3 (concurrencia) u otro flujo. |
| 2026-06-25 ~21:29 | tesorería/banking · idempotencia/correctitud (GitNexus) | FINDER halló 4. **1 fix**: `unreconcile_transaction` ancla factura al tenant (bf5a957 → **PR #52**, 58 tests, sin cambio semántico). ⚠️ #1 doble-conciliación → 2º asiento contable (inbox ALTA, necesita guard/UNIQUE), #3 auto-match por solo-importe casa factura equivocada, #4 saldo proyección suma todas las cuentas → inbox. Próx: nuevo flujo. |
| 2026-06-25 ~21:38 | importación tabular (CSV/Excel/JSON) · L1+L4+L2 (GitNexus) | FINDER halló 4. **1 fix**: `parse_tabular_file` sin tope de filas en NINGÚN formato → DoS de memoria (49 MB densos = millones de filas en RAM → `apply_import` 1 transacción). FIXER añadió `MAX_TABULAR_ROWS=50_000` que RECHAZA (no trunca, evita pérdida silenciosa) en CSV/Excel/JSON, con bail TEMPRANO en Excel para no materializar (c7a79e7 → **PR #52**, ruff verde, **81 tests pasan**). #2 "atomicidad" REFUTADO (constructor lanza antes de `add`; commit todo-o-nada). ⚠️ #1 `erp_import` no deduplica → re-import duplica TODO (inbox ALTA), #3 `_match_product` carga catálogo entero por línea → inbox. Próx: nuevo flujo. |
| 2026-06-25 ~22:08 | facturación recurrente / scheduler · idempotencia+correctitud (GitNexus) | FINDER halló 3 reales + 1 ok. **0 fix** (todo crea facturas/fechas = línea roja fiscal → inbox). VERIFICADO en código (no falsos positivos): ⚠️ **#1 ALTA** `_process_recurring_invoices` sin guard por (plantilla,periodo) + commit al final → reinicio/doble disparo genera facturas DUPLICADAS del mismo periodo (doble cobro + nº REC consumidos); fix = commit por plantilla + skip `last_run_date==today` + UNIQUE. #2 fechas: scheduler usa `timedelta(30/90/365)` fijos vs `run_recurring` usa `_add_months` real → deriva (31-ene→2-mar en vez de 28-feb). #3 TZ servidor no tenant (baja). #4 estados correcto. → inbox `facturacion-recurrente`. Próx: nuevo flujo. |
| 2026-06-25 ~22:32 | gastos / IVA soportado deducible (303) · L1 correctitud (GitNexus) | FINDER halló 4. **0 fix** (todo fiscal → inbox). VERIFICADO en código, separando lo real de lo condicional/intencional: ⚠️ **#1** `casillas_303.py:226` casilla 45 = 29+37 OMITE la 31 (bienes inversión, editable pero no suma) → si el usuario rellena la 31, infradeduce (media-alta; depende de si el frontend recalcula). **#4** `fiscal.py:87` None→21% fabrica IVA soportado en exentos (media, CONDICIONAL a nullability). **#2** sin campo `pct_deducible`: deduce 100% siempre (vehículo 50%/mixto/atenciones) — riesgo alto pero limitación de DISEÑO (migración+UI). **#3 REFUTADO**: borradores en deducible son intencionales+documentados (`preventive_check` avisa). → inbox `iva-deducible-303`. Próx: nuevo flujo. |
| 2026-06-25 ~21:12 | cálculo nómina · L1 correctitud (GitNexus) | ⭐ **Mejor fix de la v2**: `update_payroll` recalculaba con defaults (jornada 100%/12 pagas/sin IT) ignorando la ficha → al editar la nómina de un empleado a media jornada el **neto se duplicaba** (785→1570). TEST-FIRST confirmó el bug (xfail), FIXER lo arregló usando `calc_payroll_for_employee` (+bonus: `gross_salary` no se actualizaba), evaluador PASS (jornada completa idéntica, sin regresión), test ahora VERDE. commits 27c8219+78e9125 → **PR #52**. #2/#3/#4 nómina → inbox. Próx: nuevo flujo. |
| 2026-06-25 ~21:01 | informes/modelos · L6 perf (GitNexus) | FINDER halló N+1 en `build_modelo_347_data` (2N queries de Client; el 349/100/190 del mismo módulo ya usaban `.in_()`). **1 fix** perf: batch-load 1 query, resultado BYTE-IDÉNTICO (evaluador PASS punto por punto, 94 tests; commit 03537d2 → **PR #52**). #2 carga no acotada → inbox-humano; #3 query extra en aggregation → bajo. **Vuelta de lentes 1-6 completa.** Próx: nuevo flujo, lente 1. |
| 2026-06-25 ~20:54 | test-gaps · L5 | TEST-FIRST: 2 tests de regresión para fixes de seguridad sin cobertura (reset_password rechaza is_active=False; claim UUID malformado → 401). Ambos PASAN, no-tautológicos (romperían si se revierte el fix). commit 3b2733e → **PR #52**. Próx: lente 6 (perf/recursos) u otro flujo. |
| 2026-06-25 ~20:44 | envío AEAT VeriFactu · L4 errores/red (GitNexus) | FINDER riguroso (descartó 3 falsos positivos: httpx cubre timeouts, fd temporal limpio). **1 fix**: `generate_alta_xml` usa `scalar_one_or_none` + error controlado (6d7d3bb → **PR #52**, 225 tests, XML byte-idéntico; sin evaluador por defensivo verificado). #4 estados de envío AEAT / `verifactu_status` nunca escrito → inbox (no wired aún). Próx: lente 5 (test-gaps) u otro flujo. |
| 2026-06-25 ~20:38 | stock/inventario · L3 concurrencia (GitNexus) | ⚠️⚠️ **Hallazgo top de la noche**: patrón de race read-modify-write sin lock en TODA la ruta de ventas → SOBREVENTA. 4 instancias (commands.create_stock_movement [alta], pos.checkout [alta], stock_service.transfer [media], albarán idempotencia [media]). NO auto-fix (ruta dinero + coordinado multi-fichero + concurrencia no testeable en SQLite). → inbox `STOCK-concurrencia-ALTA` con fix exacto (UPDATE atómico + UNIQUE migración). Próx: lente 4 (errores/red) u otro flujo. |
