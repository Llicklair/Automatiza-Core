# Gap analysis SIF/VeriFactu (COMPLETO + reconciliado) — RD 1007/2023 + Orden HAC/1177/2024

Generado 2026-07-21. 10 areas con verificacion adversarial (20/20 agentes). tpv-f2 reconciliado del primer pase (su verificado echo-fallo).
Rama master, nucleo SIF intacto.

**Resumen consolidado:** CUMPLE=4, PARCIAL=22, FALTA=20 (total 46 items evaluados).

| Area | Items | CUMPLE | PARCIAL | FALTA |
|---|---|---|---|---|
| registro-alta | 4 | 1 | 3 | 0 |
| registro-anulacion | 2 | 0 | 2 | 0 |
| encadenamiento | 7 | 2 | 5 | 0 |
| qr | 1 | 0 | 1 | 0 |
| eventos | 3 | 0 | 0 | 3 |
| remision | 4 | 0 | 3 | 1 |
| conservacion | 2 | 0 | 2 | 0 |
| integridad | 6 | 1 | 5 | 0 |
| declaracion | 12 | 0 | 0 | 12 |
| tpv-f2 | 5 | 0 | 1 | 4 |

## registro-alta

### [PARCIAL] Generacion automatica del registro de alta simultanea o inmediatamente anterior a la expedicion (no puede existir factura emitida sin registro de alta)
- Evidencia: VERIFICADO adversarialmente. Las 3 rutas principales encadenan ANTES del commit, atomico con la factura: create_invoice (backend/app/services/billing/commands.py:132-146: maybe_append en :136 y commit en :139), create_rectificativa (commands.py:250-256) y run_recurring (commands.py:657-662), via maybe_append_verifactu_record (backend/app/services/billing/verifactu_chain.py:275-309) con lock advisory por tenant (verifactu_chain.py:227-232), idempotencia por invoice_id (verifactu_chain.py:221-225) y encadenado por enlace-hash inmune a timestamps (find_tail_huella :169-186). PERO el requisito NO se cumple en general: (a) el modo por defecto es no_remission (verifactu_mode.py:18 DEFAULT_MODE; get_mode :21-25 devuelve el default sin fila) y should_remit (:66-68) hace que maybe_append devuelva None SIN crear registro (verifactu_chain.py:293-294) -> por defecto NINGUNA factura genera alta; (b) tenant sin NIF -> se OMITE con solo un warning y return None (verifactu_chain.py:296-307), no se bloquea la emision; (c) multiples rutas que crean invoice_type='issued' y NUNCA llaman a la cadena, confirmado por grep de callers (solo commands.py:136,254,662 y tests): services/sales/commands.py:339-349, services/workflow/approval_actions.py:227-236, workers/tasks_scheduler.py:374-380, y una ADICIONAL no listada por el auditor pero verificada leyendo el codigo: workers/_orchestrator_context.py:421-433 (invoice_type='issued', status='draft', sin maybe_append); ademas services/migration/bulk_import.py:369-382 crea invoice_type=itype (puede ser 'issued') sin cadena (invoice_import.py:219 en cambio es 'received', correctamente fuera); (d) update_status (commands.py:264-286) transiciona draft->sent con solo commit, sin crear registro. delete_invoice sí bloquea el borrado si hay registro (commands.py:336-338), pero no fuerza su creacion. La existencia de backfill_verifactu.py confirma facturas sin registro a reparar a posteriori.
- Gap: Cubrir TODAS las rutas de creacion/expedicion de facturas emitidas (sales/commands.py:339, approval_actions.py:227, tasks_scheduler.py:374, _orchestrator_context.py:421, bulk_import.py:369 issued) con maybe_append_verifactu_record; bloquear (no solo avisar) la emision sin NIF de emisor; y resolver el modo no_remission: el RD 1007/2023 exige registro de alta tambien en modalidad NO VERI*FACTU (firmado), no permite omitirlo, luego el default no_remission deja hoy sin alta a todas las facturas emitidas.

### [PARCIAL] Contenido minimo del registro de alta (Art. 10.1 RD 1007/2023: obligado, destinatario, tercero/destinatario expedidor, num/serie, fecha, tipo factura, desglose, exencion/no sujecion, encadenamiento, ID del SIF, fecha-hora-minuto-segundo)
- Evidencia: VERIFICADO leyendo el builder completo. Presentes en build_registro_alta_xml (backend/app/services/billing/registro_facturacion.py:256-310): NIF+nombre del obligado via Cabecera/ObligadoEmision (:140-144) y NombreRazonEmisor (:289), IDFactura con IDEmisor/NumSerie/Fecha (:285-288), TipoFactura (:290), rectificativa TipoRectificativa+FacturasRectificadas (:293-300), Desglose por tipo de IVA (:230-241 agrupando por tax_percentage, restando descuento :208-211), CuotaTotal/ImporteTotal (:303-304), Encadenamiento (PrimerRegistro='S' o RegistroAnterior con IDEmisor/serie/fecha/huella, :160-180,:305), SistemaInformatico completo (:147-157,:306), FechaHoraHusoGenRegistro con segundos (:307; ISO con huso sin microsegundos en verifactu_chain.py:40-47), TipoHuella y Huella (:308-309). AUSENTES confirmados (leido todo el builder, no solo grep): NO se emite bloque Destinatarios (NIF/NombreRazon del cliente), obligatorio en F1 completa; NO hay indicador ni datos de expedicion por tercero/destinatario (sin EmitidaPorTercerosODestinatario ni bloque Tercero); CalificacionOperacion fija a 'S1' sujeta-no-exenta (registro_facturacion.py:52 constante, :236 cableada en _desglose), sin soporte de OperacionExenta/CausaExencion (E1-E6) ni N1/N2. El payload canonico hasheado solo cubre 8 campos (verifactu_chain.py:71-80).
- Gap: Anadir al builder el bloque Destinatarios (NIF/NombreRazon del cliente cuando RD 1619/2012 lo exige), el indicador y datos de expedicion por tercero/destinatario, y el soporte de operaciones exentas/no sujetas (CalificacionOperacion N1/N2, OperacionExenta con causa E1-E6) alimentado desde datos reales de la factura, en vez del S1 fijo.

### [CUMPLE] Todos los importes en euros; conversion si la factura se expide en otra divisa
- Evidencia: RESISTE la refutacion adversarial. El sistema es monomoneda EUR por construccion: verificado que el modelo Invoice (backend/app/db/models/billing.py:41-117) NO tiene columna currency/divisa/exchange_rate; los importes son Numeric(10,2) (:75-77). Grep 'currency|divisa|moneda|exchange_rate|tipo_cambio' sobre backend/app/db/models/billing.py y backend/app/services/billing/ no devuelve ninguna referencia de negocio (solo literales hardcodeados InvoiceCurrencyCode/TaxCurrencyCode='EUR' en facturae.py:130,:157-158). El registro de alta toma directamente invoice.tax_amount e invoice.amount_total (verifactu_chain.py:243-244) formateados con 2 decimales por _fmt_importe (:30-32), sin ninguna logica de conversion. No existe ruta para expedir una factura emitida en otra divisa (ni columna donde guardarla), luego la rama de conversion es inalcanzable y el ImporteTotal/CuotaTotal del registro es necesariamente EUR. La unica lectura de 'currency' externa (services/ocr/invoice_scanner.py:226) afecta a facturas RECIBIDAS (invoice_import.py:229 type='received'), que no generan registro de alta, y ni siquiera se persiste (no hay columna).

### [PARCIAL] Registro en XML UTF-8 con la estructura del bloque RegistroAlta (anexo Orden HAC/1177/2024, apartados 2.3 y 3)
- Evidencia: VERIFICADO. El XML usa la estructura oficial RegFactuSistemaFacturacion/RegistroFactura/RegistroAlta con los targetNamespace literales AEAT sin version (registro_facturacion.py:35-46 NS_LR/NS_SF, :279-282), respeta el orden estricto de elementos del XSD (:284-309) y se serializa en UTF-8 con declaracion XML (:126-127 tostring(encoding='utf-8', xml_declaration=True)). validate_verifactu_xml (:353-396) valida contra los XSD oficiales en backend/app/services/aeat/xsd/SuministroLR.xsd con resolver offline para xmldsig-core-schema.xsd (:380-386). Sin embargo, el contenido no cubre los bloques condicionalmente obligatorios del apartado 2.3 (Destinatarios, OperacionExenta, Subsanacion/RechazoPrevio, Macrodato, FacturaSinIdentifDestinatarioArt61d) y ClaveRegimen/CalificacionOperacion van fijos a 01/S1 (:51-52 constantes, :235-236 cableadas); como el XSD los admite con minOccurs=0, la validacion PASA aunque el contenido este incompleto para esos supuestos, dando una falsa sensacion de conformidad. La conformidad estructural/encoding en si esta resuelta.
- Gap: Completar los bloques condicionales del anexo (Destinatarios, exenciones, Subsanacion, Macrodato) y hacer ClaveRegimen/CalificacionOperacion dependientes de los datos reales de la operacion; la validacion XSD actual no detecta su ausencia por ser minOccurs=0.

## registro-anulacion

### [PARCIAL] Cuando se anule un registro de facturacion, el SIF debe generar un registro de facturacion de anulacion con los datos identificativos de la factura anulada, los datos de encadenamiento y la fecha-hora-minuto-segundo de generacion.
- Evidencia: VERIFICADO. Los builders existen y contienen todos los datos exigidos: build_payload_anulacion (backend/app/services/billing/verifactu_chain.py:84-104) emite IDEmisorFacturaAnulada, NumSerieFacturaAnulada, FechaExpedicionFacturaAnulada, Huella (anterior) y FechaHoraHusoGenRegistro; la precision de segundos la da _fmt_fecha_hora_gen (verifactu_chain.py:40-47, .replace(microsecond=0).isoformat()). El XML build_registro_anulacion_xml (backend/app/services/billing/registro_facturacion.py:313-347) emite IDFactura anulada (:338-341), Encadenamiento (:342 via _encadenamiento :160-180: PrimerRegistro='S' o RegistroAnterior con 4 campos) y FechaHoraHusoGenRegistro (:344). PERO la GENERACION en produccion esta 100% ausente: grep de 'anulacion|RegistroAnulacion|tipo_registro' en TODO backend/app/ (excluyendo los 2 archivos builder) no devuelve NADA; los unicos callers de build_payload_anulacion/build_registro_anulacion_xml son tests (backend/tests/test_verifactu_chain.py:110 y backend/tests/test_verifactu_registro_xml.py:112). maybe_append_verifactu_record solo se invoca en flujos de ALTA (commands.py:136 crear, :254 rectificativa que encadena RegistroAlta tipo R, :662 finalizar). El flujo real de anulacion no genera registro: update_status permite pasar a 'cancelled' sin emitir nada (commands.py:264-314, solo setea status y hace commit; unico evento es invoice_paid); delete_invoice bloquea el borrado si hay VerifactuRecord y redirige a rectificativa (commands.py:336-348). El propio docstring lo admite: 'la anulacion no se persiste como VerifactuRecord todavia' (registro_facturacion.py:327). BLOQUEO EXTRA no citado por el auditor: VerifactuRecord.invoice_id es unique=True (backend/app/db/models/billing.py:135), asi que ni siquiera se podria persistir una anulacion como eslabon de la misma factura ya registrada sin cambiar el esquema. Casos limite: (a) primer registro de la cadena — una anulacion nunca puede ser genesis legitimo, y el campo SinRegistroPrevio (XSD :192) que cubriria 'anulacion sin registro previo' no se emite; (b) factura anulada dos veces — no hay idempotencia ni persistencia, y el unique(invoice_id) lo impediria.
- Gap: Cablear la generacion real: al anular (transicion a 'cancelled' o flujo dedicado) de una factura con VerifactuRecord, construir el payload de anulacion, calcular su huella, y persistirlo como nuevo eslabon append-only de la cadena. Requiere (1) distinguir alta/anulacion en el modelo (campo tipo_registro + migracion) y (2) ELIMINAR/relajar el unique(invoice_id) de VerifactuRecord (billing.py:135) que hoy impide un segundo registro para la misma factura, y (3) encolar el envio en verifactu_submit.

### [PARCIAL] El registro de anulacion debe generarse en formato XML, codificacion UTF-8, con la estructura del bloque «RegistroAnulacion» (apartado 2.4 y 3 del anexo).
- Evidencia: VERIFICADO. Cuando el builder se ejecuta produce XML correcto: bloque sf:RegistroAnulacion en los namespaces oficiales (backend/app/services/billing/registro_facturacion.py:335), serializado UTF-8 con declaracion XML (_serialize :126-127, tostring(encoding='utf-8', xml_declaration=True) -> <?xml ... encoding='utf-8'?>), y con el orden de elementos que exige el XSD RegistroFacturacionAnulacionType (backend/app/services/aeat/xsd/SuministroInformacion.xsd:184-207: IDVersion -> IDFactura -> [opcionales] -> Encadenamiento -> SistemaInformatico -> FechaHoraHusoGenRegistro -> TipoHuella -> Huella). El builder :337-346 emite IDVersion, IDFactura, Encadenamiento, SistemaInformatico, FechaHoraHusoGenRegistro, TipoHuella, Huella en ese orden y valida limpio contra el XSD (test_anulacion_valida_xsd, backend/tests/test_verifactu_registro_xml.py:110-124, assert validate_verifactu_xml(xml)==[]). Validacion XSD implementada en validate_verifactu_xml (registro_facturacion.py:353-396) con esquemas en backend/app/services/aeat/xsd/. LIMITACIONES CONFIRMADAS: (a) es codigo nunca invocado en produccion (solo tests), por lo que el SIF no genera este XML en ninguna anulacion real — el 'debe generarse' del requisito no ocurre; (b) el builder NO emite ninguno de los 5 campos opcionales del bloque definidos en el XSD (SuministroInformacion.xsd:191-195: RefExterna, SinRegistroPrevio, RechazoPrevio, GeneradoPor, Generador) — validan por ser minOccurs=0, pero sin SinRegistroPrevio/RechazoPrevio no se cubren los escenarios del apartado 2.4 (anulacion sin registro previo / rechazo previo).
- Gap: Integrar build_registro_anulacion_xml en el flujo real de anulacion (hoy nunca se genera) y anadir soporte para los campos opcionales del bloque (al menos SinRegistroPrevio y RechazoPrevio/GeneradoPor-Generador) para cubrir los escenarios del apartado 2.4 del anexo.

## encadenamiento

### [PARCIAL] Cada registro de alta y de anulacion debe incorporar del registro inmediatamente anterior: NIF del emisor, numero de serie y numero de factura, fecha de expedicion, y los primeros 64 caracteres de su huella o hash.
- Evidencia: ALTAS: verificado. registro_facturacion.py:176-180 (_encadenamiento) emite RegistroAnterior con IDEmisorFactura, NumSerieFactura, FechaExpedicionFactura y Huella tomados de prev_record; generate_alta_xml (registro_facturacion.py:413-421) recupera prev_record por su huella (== record.huella_anterior) y si hay huella_anterior sin prev_record lanza ValueError (172-175). Huella es SHA-256 hex de 64 chars completos (columna huella String(64) NOT NULL en db/models/billing.py:137). Test que valida el XML contra el XSD oficial y asegura RegistroAnterior+huella previa: tests/test_verifactu_registro_xml.py:92-106; test negativo del prev_record faltante: 211-217. ANULACIONES: no existe el flujo. build_registro_anulacion_xml (registro_facturacion.py:313-347) y build_payload_anulacion (verifactu_chain.py:84-104) no tienen ningun caller en app/ (grep de ambos simbolos: solo su definicion, los XSD y un test unitario aislado, tests/test_verifactu_chain.py:108-121). Confirmado ademas que el ERP NO anula facturas emitidas: commands.py:343-347 rechaza la anulacion y obliga a emitir una rectificativa. El propio docstring lo admite: 'la anulacion no se persiste como VerifactuRecord todavia' (registro_facturacion.py:327).
- Gap: Implementar el flujo real de anulacion: generar y persistir el registro de anulacion encadenado (con RegistroAnterior) cuando se anula una factura; hoy no hay ruta ni servicio que lo invoque.

### [CUMPLE] El primer registro de facturacion generado por el SIF es la unica excepcion al encadenamiento: debe marcarse como primer registro (elemento PrimerRegistro del bloque Encadenamiento) en lugar de referenciar un registro anterior.
- Evidencia: Verificado y resiste lectura adversarial. registro_facturacion.py:168-170: si no hay huella_anterior, _encadenamiento emite <PrimerRegistro>S</PrimerRegistro> y NO genera RegistroAnterior. El valor 'S' es el unico admitido por el XSD oficial (SuministroInformacion.xsd:785-789, simpleType PrimerRegistroCadenaType con enumeration unica 'S') y Encadenamiento es un choice estricto PrimerRegistro|RegistroAnterior (SuministroInformacion.xsd:167-174, y 196-203 para anulacion). Unicidad de la genesis garantizada: append_verifactu_record solo produce huella_anterior=None cuando _get_last_huella devuelve None por ausencia de registros del tenant (verifactu_chain.py:189-201); el pg_advisory_xact_lock por tenant (228-232) mas la idempotencia por invoice_id (221-225) impiden dos genesis concurrentes; el backfill no crea una segunda genesis (backfill_verifactu.py:154-162 encadena tras find_tail_huella si ya existe cadena). Los triggers append-only de la migracion 0011 (rechazan UPDATE y DELETE, 0011_verifactu_chain.py:55-74) impiden corromper la cadena para fabricar una genesis extra. En el payload hasheado la huella anterior vacia se representa como 'Huella=' (verifactu_chain.py:78). Test: tests/test_verifactu_registro_xml.py:81-88 valida contra el XSD oficial y assert 'PrimerRegistro' in xml; tests/test_verifactu_chain.py:147-161 confirma huella_anterior None en el primer registro.

### [PARCIAL] Para cada obligado tributario, cada SIF debe producir una UNICA cadena de registros de facturacion, con altas y anulaciones intercaladas en la misma cadena.
- Evidencia: Cadena unica por obligado: SI para altas. Se encadena solo por tenant_id, sin particionar por serie/tipo/terminal: _get_last_huella filtra solo por tenant_id (verifactu_chain.py:197-201), el lock es por tenant ('verifactu:{tenant_id}', 228-232) y la serie es solo campo de display (serie inferida en 238-239, sin entrar en el encadenado). order_verifactu_chain (120-166) detecta multiples genesis/bifurcaciones como cadena mal formada. PERO las anulaciones nunca entran en la cadena: no existe ningun punto que cree un VerifactuRecord de anulacion (build_payload_anulacion sin callers; los unicos append son altas via maybe_append_verifactu_record; commands.py:343-347 fuerza rectificativa en lugar de anulacion). Por tanto la 'unica cadena con altas y anulaciones intercaladas' solo contiene altas. Nota adicional verificada (no altera el estado): si el backfill se ejecuta DESPUES de existir registros en tiempo real, encadena las facturas historicas tras la cola actual (backfill_verifactu.py:155-206), invirtiendo el orden cronologico dentro de la misma cadena unica.
- Gap: Al implementar la anulacion, persistirla como registro en verifactu_chain del MISMO tenant, tomando huella_anterior de la cola comun (find_tail_huella), para que altas y anulaciones queden intercaladas en una sola cadena.

### [PARCIAL] La huella del alta se calcula sobre: NIF emisor; numero de factura y serie; fecha de expedicion; tipo de factura; cuota total; importe total; huella anterior; y fecha-hora-huso de generacion. La de anulacion: NIF emisor, numero y serie, fecha de expedicion, huella anterior y fecha-hora-huso.
- Evidencia: ALTA: verificado. build_payload_alta (verifactu_chain.py:50-81) usa exactamente los 8 campos en el orden oficial (IDEmisorFactura, NumSerieFactura, FechaExpedicionFactura, TipoFactura, CuotaTotal, ImporteTotal, Huella anterior, FechaHoraHusoGenRegistro) y ningun campo extra entra en el hash; el orden esta blindado por test (tests/test_verifactu_chain.py:86-93). append_verifactu_record (246-256) lo alimenta con NIF del tenant, invoice_number (serie+numero), fecha dd-mm-yyyy, F1/R1, tax_amount y amount_total. ANULACION: build_payload_anulacion (84-104) define el subconjunto correcto (IDEmisorFacturaAnulada, NumSerieFacturaAnulada, FechaExpedicionFacturaAnulada, Huella, FechaHoraHusoGenRegistro), pero es codigo muerto: sin callers en app/, solo lo ejercita un test unitario (tests/test_verifactu_chain.py:108-121); no hay flujo que calcule ni persista esa huella.
- Gap: Conectar build_payload_anulacion a un flujo real de anulacion que calcule y persista la huella del registro de anulacion; hoy solo esta definida la funcion.

### [CUMPLE] La huella se calcula con SHA-256 siguiendo el detalle de concatenacion de campos y representacion publicado por la AEAT (primeros 64 caracteres encadenados).
- Evidencia: Verificado computacionalmente y resiste lectura adversarial. compute_huella (verifactu_chain.py:107-112): SHA-256 del payload UTF-8, hex en MAYUSCULAS (64 chars). Formato canonico clave=valor unido por '&' sin espacios (71-81), importes con punto y 2 decimales (_fmt_importe 30-32), fecha dd-mm-yyyy (35-37), huella anterior vacia como 'Huella=' (78). Reproduje de forma independiente el vector oficial AEAT de la cadena de ejemplo y obtuve exactamente 3C464DAF61ACB827C65FDA19F352A4E3BDC2C640E9E9FC4CC058073F38F12F60, identico al esperado en tests/test_verifactu_chain.py:52-74. TipoHuella=01 (SHA-256) declarado en el XML (registro_facturacion.py:49,308). Lo encadenado es la huella completa de 64 chars (huella_anterior String(64), db/models/billing.py:138), que coincide con 'los primeros 64 caracteres' al ser SHA-256 hex exactamente 64. Caveat sin impacto en el estado del metodo: la representacion canonica de la ANULACION (build_payload_anulacion) no esta validada contra ningun vector oficial y es codigo muerto, pero el metodo de hash queda demostrado por el vector de alta.

### [PARCIAL] El SIF debe incorporar a cada registro la fecha y hora exactas de generacion segun el territorio de expedicion, con margen maximo de error de un minuto, incluyendo el huso horario aplicado.
- Evidencia: En emision normal la hora es real y lleva huso: verifactu_chain.py:254 fecha_hora_gen=_fmt_fecha_hora_gen(datetime.now(UTC).astimezone()); _fmt_fecha_hora_gen (40-47) emite ISO 8601 con offset y sin microsegundos. Tres carencias confirmadas: (1) el huso es el de la zona LOCAL DEL SERVIDOR (astimezone() sin argumento en 254), no el del territorio de expedicion; no existe ZoneInfo('Europe/Madrid') ni configuracion de tz por tenant (grep sin resultados en services/billing y core/config); un despliegue con TZ=UTC emitiria +00:00. (2) No hay control de exactitud del reloj (margen 1 minuto): sin sincronizacion/verificacion NTP ni chequeo de deriva en el codigo. (3) El backfill fabrica la fecha-hora de generacion a partir de la fecha de la factura, no de la hora real de generacion del registro: backfill_verifactu.py:187 fecha_hora_gen=_fmt_fecha_hora_gen(invoice.date).
- Gap: Fijar el huso al territorio de expedicion (p.ej. ZoneInfo('Europe/Madrid') o tz configurable por tenant) en lugar de la tz del SO; documentar/implementar control de deriva de reloj (NTP) para el margen de 1 minuto; revisar que FechaHoraHusoGenRegistro del backfill refleje la generacion real del registro.

### [PARCIAL] El SIF debe permitir el seguimiento de la secuencia de la cadena de registros hacia delante y hacia atras, de forma rapida, facil e intuitiva.
- Evidencia: La maquinaria interna existe pero no esta expuesta. order_verifactu_chain (verifactu_chain.py:120-166) reconstruye la secuencia genesis->cola siguiendo el enlace hash en ambos sentidos y verify_chain_integrity (312-338) la recorre y valida. Verificado por grep que ninguna de las dos tiene caller en app/api ni services: order_verifactu_chain solo se usa internamente (verifactu_chain.py:185,326) y verify_chain_integrity no se invoca fuera de tests. El unico endpoint publico, GET /api/v1/verify/{huella} (api/v1/routes/verify.py:27-64), devuelve UN registro con su huella_anterior (un solo paso hacia atras), sin enlace hacia delante (no localiza el sucesor) ni listado ordenado de la cadena. No hay endpoint ni pantalla de 'recorrer la cadena'.
- Gap: Exponer el seguimiento: endpoint (y/o vista) que liste la cadena del tenant ordenada por enlace (reutilizando order_verifactu_chain) con navegacion siguiente/anterior, y/o invocar verify_chain_integrity desde una ruta de auditoria; anadir enlace hacia delante en /verify/{huella}.

## qr

### [PARCIAL] Toda factura (impresa o imagen digital) debe incluir un codigo QR legible y, si opera como VERI*FACTU, la frase «Factura verificable en la sede electronica de la AEAT» o «VERI*FACTU» con tipo y tamano de letra similares al resto de datos (Art. 20.1 HAC/1177/2024).
- Evidencia: VERIFICADO leyendo el codigo, no solo su existencia. Existe un bloque QR REAL y conectado (no es codigo muerto): build_invoice_pdf (backend/app/services/billing/queries.py:214-233) llama a _load_verifactu (229) -> _build_invoice_data pone la clave 'verifactu' (185) -> generate_invoice_pdf la pasa a _verifactu_qr_block (backend/app/services/pdf/invoices.py:177) -> _invoice_sections.py:328-353 renderiza un QrCodeWidget de 25mm apuntando a verify_url. Por tanto NO es FALTA: cuando hay registro y URL, el QR sale y funciona. PERO no cumple el Art. 20.1 por tres defectos confirmados: (1) OPCIONAL Y SILENCIOSO — _verifactu_qr_block devuelve [] si falta verifactu/huella/verify_url (_invoice_sections.py:336-341) y su docstring dice 'la factura sigue siendo valida sin QR' (333-334); _load_verifactu devuelve None si no hay VerifactuRecord (queries.py:138-139) O si FRONTEND_URL esta vacio (141-142), y ADEMAS nunca consulta el modo del tenant (voluntary/no_remission), de modo que una factura emitida en modo VERI*FACTU con FRONTEND_URL sin configurar produce un PDF sin QR y sin aviso alguno. (2) LEYENDA LEGAL AUSENTE — la leyenda impresa es 'Verifactu — Escanea para verificar' (_invoice_sections.py:373), no «Factura verificable en la sede electronica de la AEAT» ni «VERI*FACTU»; grep de 'VERI\*FACTU|verificable en la sede' en backend/app solo aparece en aeat/xsd/EventosSIF.xsd y en la migracion 0023_verifactu_config.py (comentarios), nunca en el renderizador PDF. (3) TAMANO DE LETRA MENOR — caption a 7pt (caption_sty, _invoice_sections.py:358) y huella a 6pt (huella_sty, 366), por debajo del cuerpo de la factura a 9pt (body_sty, invoices.py:88), incumpliendo 'tamano de letra similar al resto de datos'.
- Gap: Hacer el QR + leyenda OBLIGATORIOS en toda factura emitida bajo modo VERI*FACTU: _load_verifactu debe consultar el modo del tenant y, en modo voluntary, FALLAR la generacion del PDF (o del registro) si falta VerifactuRecord o FRONTEND_URL en lugar de devolver None y omitir el bloque en silencio. Sustituir la leyenda 'Verifactu — Escanea para verificar' por el texto legal exacto «Factura verificable en la sede electronica de la AEAT» o «VERI*FACTU». Elevar el tamano de la leyenda (y del identificador visible) a un tipo/tamano similar al resto de datos de la factura (>=9pt como body_sty).

## eventos

### [FALTA] El SIF debe detectar y registrar al menos estos eventos: inicio y fin de funcionamiento como NO VERI*FACTU; lanzamiento del proceso de deteccion de anomalias en registros de facturacion y deteccion de anomalias en ellos; lanzamiento y deteccion de anomalias en registros de evento; restauracion de copia de seguridad gestionada desde el SIF; exportacion de registros de facturacion de un periodo; y exportacion de registros de evento de un periodo.
- Evidencia: No existe ningun registro de eventos del SIF. Busquedas exhaustivas en todo backend/ de 'RegistroEvento|EventosSIF|TipoEvento|registro_evento|verifactu_event' solo devuelven el XSD oficial (backend/app/services/aeat/xsd/EventosSIF.xsd, 823 lineas, con TipoEventoType en linea 408) y el script que lo descarga (backend/scripts/update_aeat_xsd.py) — ningun .py de aplicacion lo importa ni lo usa. Los unicos modelos VeriFactu son VerifactuRecord (backend/app/db/models/billing.py:119, registros de facturacion) y VerifactuConfig (billing.py:155); no hay modelo ni migracion de eventos (migraciones 0011_verifactu_chain, 0018_verifactu_backfill_flag, 0023_verifactu_config: ninguna crea tabla de eventos). Piezas sueltas que existen pero NO registran evento alguno: (a) cambio de modo voluntary/no_remission en set_mode (backend/app/services/billing/verifactu_mode.py:41-64) solo emite logger.info (lineas 57-63), un log de aplicacion sin garantias ni huella; (b) verify_chain_integrity (backend/app/services/billing/verifactu_chain.py:312-338) detecta manipulacion de la cadena de facturacion pero solo lo invocan tests (tests/test_verifactu_chain.py:221,238; tests/test_idempotency.py:87,216; tests/test_backfill_verifactu.py:72,140) — sin lanzamiento en produccion ni registro del lanzamiento/resultado; (c) hay backup con BackupRecord (backend/app/db/models/backup.py:16) y services/backup/ pero la restauracion no genera evento; (d) no se encontro endpoint de exportacion de registros de facturacion ni de eventos por periodo (grep 'export|descarga|csv|zip|periodo' en app/api/v1/routes/verifactu_config.py — solo tiene GET linea 32 y PUT linea 47 de configuracion — y en rutas de invoices: sin resultados).
- Gap: Implementar el subsistema completo de registro de eventos: tabla/modelo de eventos SIF, servicio que registre los 6 tipos obligatorios del art. 9.1 (alta/baja de funcionamiento NO VERI*FACTU en set_mode, lanzamiento+resultado de verify_chain_integrity conectado a un scheduler real, deteccion de anomalias sobre el propio log de eventos, hook en la restauracion de backup, y endpoints de exportacion por periodo de facturacion y de eventos que ademas registren el evento de exportacion).

### [FALTA] Por cada 6 horas de operacion disponible, el SIF debe generar al menos un registro resumen de eventos sucedidos desde el resumen anterior (o desde el inicio de funcionamiento si no hay anterior); este resumen se trata como un evento registrado mas.
- Evidencia: Busque 'resumen|summary|6 hora|six hour' en backend/app/services/billing/ y backend/app/workers/ (schedulers): solo aparecen resumenes ajenos (backfill en backend/app/services/billing/backfill_verifactu.py:52, trazas de coste del orquestador en workers/tasks_orchestrator.py:210-215, cierre mensual en workers/tasks_scheduler.py:592). No existe ninguna tarea periodica de 6 horas ni concepto de 'registro resumen de eventos', coherente con que no existe el propio log de eventos (ver requisito anterior).
- Gap: Crear una tarea programada (workers/tasks_scheduler.py) que cada 6 horas de operacion genere un registro-resumen de eventos desde el resumen anterior o desde el arranque, y lo persista como un evento mas dentro de la cadena de eventos.

### [FALTA] Los registros de evento deben garantizar integridad, inalterabilidad, trazabilidad, conservacion, accesibilidad y legibilidad igual que los de facturacion, con su propia huella encadenada calculada sobre: identificador del productor, identificador del SIF, version, numero de instalacion, NIF del obligado, tipo de evento, huella del evento anterior y fecha-hora-huso de generacion.
- Evidencia: No existe cadena de eventos. La unica cadena hash del sistema es la de registros de facturacion: backend/app/services/billing/verifactu_chain.py (docstring linea 1: 'Cadena hash Verifactu (FAC.HASH) — RD 1007/2023 Art. 8'), con payload canonico construido sobre campos de factura (lineas 58-105: IDEmisorFactura, NumSerieFactura, importes, Huella=huella_anterior) y compute_huella SHA-256 (lineas 107-112), persistida en VerifactuRecord (backend/app/db/models/billing.py:119, migracion 0011_verifactu_chain.py). Ningun codigo calcula huella sobre los campos exigidos para eventos (identificador de productor, identificador del SIF, version, numero de instalacion, tipo de evento, huella del evento anterior): grep de esos conceptos asociados a eventos no devuelve nada fuera del XSD EventosSIF.xsd, que esta sin usar.
- Gap: Implementar una cadena de huellas independiente para eventos (tabla propia con huella/huella_anterior por tenant o instalacion), payload canonico con los 8 campos del art. 13.1.c (productor, SIF, version, instalacion, NIF obligado, tipo de evento, huella anterior, fecha-hora con huso), verificador de integridad analogo a verify_chain_integrity, y politica de conservacion/accesibilidad equivalente a la de facturacion.

## remision

### [PARCIAL] Remision via servicio web AEAT con XML del anexo: Cabecera (2.1) + bloques RegistroFactura (2.2) repetibles (art. 16.1 y anexo ap. 4 Orden HAC/1177/2024)
- Evidencia: El XML RegFactuSistemaFacturacion se genera y XSD-valida de verdad: raiz + Cabecera/ObligadoEmision + RegistroFactura en build_registro_alta_xml (registro_facturacion.py:279-282, _cabecera 140-144) y anulacion (332-335), validado contra SuministroLR.xsd via lxml con resolver offline en validate_verifactu_xml (registro_facturacion.py:353-396); XSDs oficiales presentes (services/aeat/xsd/SuministroLR.xsd, SuministroInformacion.xsd, RespuestaSuministro.xsd). PERO la 'remision via servicio web' no existe end-to-end: (1) HttpxVerifactuTransport.send postea signed_xml crudo con text/xml + SOAPAction vacio (verifactu_submit.py:206-209), sin soap:Envelope/Body en ningun punto — no hay construccion de sobre SOAP en el repo; (2) el propio codigo declara el sobre SOAP 'POR CONFIRMAR' (verifactu_submit.py:15-16, 196-197) y produccion=None (linea 37); (3) siempre se emite UN solo RegistroFactura (build_registro_alta_xml:281-282), nunca repetibles; (4) el unico caller real, routes/invoices.py:537, fija confirmed=False, asi que PreproduccionSubmitter nunca hace POST (dry-run, verifactu_submit.py:250-256); (5) RegistroAnulacion se puede construir pero NINGUN codigo lo remite: submit solo llama generate_alta_xml (verifactu_submit.py:245), y build_registro_anulacion_xml (registro_facturacion.py:313) no se invoca en ningun sitio de app/ (grep RegistroAnulacion/anulacion solo XSD + definicion).
- Gap: Construir el sobre SOAP conforme al WSDL oficial (SistemaFacturacion.wsdl), confirmar endpoints contra preproduccion y habilitar produccion; soportar varios RegistroFactura por mensaje (hoy siempre 1); cablear la remision de RegistroAnulacion; y activar confirmed=True una vez homologado (hoy hardcodeado a False en la ruta).

### [FALTA] Control de flujo por tiempo de espera entre envios: valor inicial 60s, aplicar el nuevo valor devuelto en cada respuesta al siguiente envio (art. 16.2 Orden HAC/1177/2024)
- Evidencia: El control de flujo NO esta implementado; lo unico que existe es el parseo de un campo que nadie consume. grep 'tiempo_espera|TiempoEsperaEnvio' en backend/ devuelve exclusivamente: el XSD (RespuestaSuministro.xsd:25), el campo del dataclass VerifactuAck (verifactu_submit.py:66), el parseo (verifactu_submit.py:106 y 138) y un test (tests/test_verifactu_submit.py:22,92). No hay valor inicial de 60s en ninguna parte, no se persiste el TiempoEsperaEnvio devuelto por tenant/instalacion, y no existe ninguna logica de espera, throttle ni encolado entre envios. Ademas no hay remitente automatico: submit_invoice_to_verifactu solo se llama desde routes/invoices.py:537 (una factura, on-demand, confirmed=False), asi que no hay bucle de envios donde aplicar la cadencia. Parsear el campo a un dataclass que ningun codigo lee no satisface el requisito de 'control de flujo por tiempo de espera' (dato muerto), por eso se degrada de PARCIAL a FALTA.
- Gap: Persistir el ultimo TiempoEsperaEnvio por tenant/instalacion (default 60s), construir el remitente automatico que respete ese plazo antes del siguiente envio y encole los registros generados durante la espera, y consumir ack.tiempo_espera_envio para actualizar el valor.

### [PARCIAL] Procesar la respuesta distinguiendo aceptados / aceptados con errores / rechazados; subsanar y reenviar rechazados con Subsanacion='S' y RechazoPrevio='S'/'X' (art. 16 y anexo L17 Orden HAC/1177/2024)
- Evidencia: El parser SI modela y distingue los estados: EstadoEnvio (Correcto|ParcialmenteCorrecto|Incorrecto) via _direct_text (verifactu_submit.py:136) y por linea VerifactuLineAck con estado_registro, codigo_error, descripcion_error y duplicado (verifactu_submit.py:48-57, 107-132). PERO el PROCESAMIENTO ignora esa distincion: en submit_invoice_to_verifactu solo estado_envio=='correcto' marca inv.verifactu_status='sent' y cualquier otro valor marca 'error' a nivel factura (verifactu_submit.py:379-383); las lineas (ack.lineas) no se leen, no se persisten ni se actua sobre AceptadoConErrores/Incorrecto. Y el flujo de subsanacion es inexistente: grep 'Subsanacion|RechazoPrevio' en backend devuelve SOLO los XSD (SuministroInformacion.xsd:109-110,193,1371-1372; RespuestaConsultaLR.xsd:86-87), 0 ocurrencias en codigo Python — build_registro_alta_xml no emite esos campos (registro_facturacion.py:284-310) y no hay logica de reenvio de rechazados.
- Gap: Persistir el resultado por registro (estado_registro, codigo error, CSV/duplicado) actuando sobre ack.lineas; anadir al generador del RegistroAlta los campos Subsanacion y RechazoPrevio (L17) con un flujo de correccion y reenvio de los rechazados.

### [PARCIAL] En VERI*FACTU no es necesaria la firma electronica de los registros; huella + remision efectiva sustituyen a la firma del art. 12 (art. 14 Orden HAC/1177/2024)
- Evidencia: La huella SHA-256 encadenada esta implementada y los registros se guardan sin firma, coherente con art. 14: VerifactuRecord persiste huella/huella_anterior/payload_canonico (db/models/billing.py:137-139), la huella se calcula en append_verifactu_record (verifactu_chain.py:256, record 258-269) con inmutabilidad append-only via triggers PL/pgSQL que rechazan UPDATE/DELETE (migracion 0011_verifactu_chain.py:44-77) — OJO: solo en Postgres; en SQLite (tests) la inmutabilidad es solo a nivel codigo (billing.py:127-128). Primer registro correcto: PrimerRegistro='S' cuando no hay huella_anterior (registro_facturacion.py:168-169). PERO la sustitucion no se completa: (1) la 'remision efectiva' nunca ocurre (ver requisito 1: confirmed=False hardcodeado, sin sobre SOAP, sin path de anulacion); (2) el pipeline de envio EXIGE firma XAdES del XML y aborta si sale stub (verifactu_submit.py:279-284), un gate que el propio codigo marca 'POR CONFIRMAR' (lineas 15-16, 196-197) y que contradice el art. 14 (en VERI*FACTU la autenticacion del WS es via certificado mTLS, no firma del mensaje).
- Gap: Confirmar contra preproduccion que el WS no requiere XAdES del cuerpo y retirar ese bloqueo (mantener solo mTLS); completar la remision efectiva para que la sustitucion huella+remision opere de verdad.

## conservacion

### [PARCIAL] El SIF debe garantizar la conservacion, accesibilidad y legibilidad de todos los registros de facturacion generados (y de evento), permaneciendo consultables durante el plazo de prescripcion (Art. 8.1 RD 1007/2023).
- Evidencia: CONSERVACION DE REGISTROS DE FACTURACION (ALTA): real y solida, verificada leyendo el codigo. Tabla append-only con triggers PL/pgSQL que hacen RAISE EXCEPTION en UPDATE y DELETE (backend/app/db/migrations/versions/0011_verifactu_chain.py:52-77); modelo VerifactuRecord con payload_canonico, huella y huella_anterior (backend/app/db/models/billing.py:119-152). El alta encadenada funciona y esta cableada a la emision: maybe_append_verifactu_record se llama en creacion, rectificativa y aprobacion (backend/app/services/billing/commands.py:136,254,662 -> backend/app/services/billing/verifactu_chain.py:204-272); el primer registro (huella_anterior NULL) se maneja bien (build_payload_alta usa Huella='' y order_verifactu_chain trata el genesis en successors.get(None), verifactu_chain.py:78,145-148). Borrado bloqueado: delete_invoice lanza ValueError 'cadena inmutable' si hay VerifactuRecord (backend/app/services/billing/commands.py:336-338), expuesto como 409 (backend/app/api/v1/routes/invoices.py:318-320). No hay ninguna ruta que haga UPDATE de un VerifactuRecord (append-only tambien en codigo). ACCESIBILIDAD/LEGIBILIDAD: endpoint publico por huella con integrity_ok recomputado (backend/app/api/v1/routes/verify.py:44-64) y huella+verify_url inyectada en el PDF (backend/app/services/billing/queries.py:136-146) son reales. Backup dedicado 'verifactu' con SHA-256 registrado (backend/app/api/v1/routes/backup_local.py:24-27), aunque el dump fisico lo hace el cliente Electron y el backend solo guarda metadatos (backend/app/services/backup_local.py:1-11,30-32) -> es voluntario. REFUTACIONES A LA EVIDENCIA DEL AUDITOR: (a) es FALSO que la verificacion de integridad de la cadena completa este 'cableada al health-check (system.py:153)': verify_chain_integrity (verifactu_chain.py:312-338) solo se invoca desde tests (grep: cero llamadas en backend/app), y system.py:153 llama a check_invoice_preconditions (backend/app/services/system/preconditions.py:18-41) que UNICAMENTE comprueba que la tabla verifactu_chain existe via inspector.get_table_names(), NO verifica huellas ni encadenado; la verificacion full-chain no esta operativamente conectada a ningun endpoint/health. (b) build_payload_anulacion (verifactu_chain.py:84-104) esta definida pero NUNCA se llama (grep: solo la def) -> no se genera ni conserva ningun registro de anulacion; la cancelacion se hace por rectificativa R1 (verifactu_chain.py:242). CONFIRMADO como FALTA: (1) registros de EVENTO inexistentes -- grep de RegistroEvento/EventosSIF/sif_event en backend/app solo devuelve el XSD (backend/app/services/aeat/xsd/EventosSIF.xsd), sin tabla ni servicio que los genere/conserve; (2) sin gestion de plazo de prescripcion -- grep 'prescripci|retention' solo da retencion IRPF y BACKUP_RETENTION_DAYS=7 (backend/app/core/config.py:136); (3) los triggers append-only solo existen en Postgres, en SQLite la inmutabilidad es solo por codigo (0011_verifactu_chain.py:49-51,8-10).
- Gap: Implementar el subsistema de registros de evento (deteccion/generacion/conservacion encadenada segun EventosSIF.xsd), hoy inexistente. Cablear la verificacion full-chain (verify_chain_integrity) a un health-check/endpoint operativo, no solo a tests. Garantizar/documentar la conservacion durante el plazo de prescripcion (hoy depende de que el usuario no pierda la BD y de backups voluntarios del cliente Electron). Replicar la proteccion append-only fuera de Postgres si se soporta otro motor en produccion. Opcional: generar registro de anulacion (build_payload_anulacion esta escrita pero sin cablear).

### [PARCIAL] A requerimiento de la Administracion, poder suministrar los registros de facturacion conservados mediante envio automatico y seguro por medios electronicos a la sede electronica (Art. 14.2 RD 1007/2023).
- Evidencia: Existe maquinaria real de envio seguro pero pertenece a un flujo DISTINTO y esta inerte; la capacidad de suministro a requerimiento NO existe. Lo real: transporte SOAP con mTLS (HttpxVerifactuTransport.send, backend/app/services/billing/verifactu_submit.py:195-214), validacion contra XSD oficial (verifactu_submit.py:245-248), firma XAdES con el cert del tenant (verifactu_submit.py:279-284), y parseo del acuse real -- verificado, nunca finge envio (verifactu_submit.py:357-392). PERO al leer el codigo: (1) ese pipeline es la REMISION VeriFactu factura-a-factura del ALTA en el momento de emitir (submit_invoice_to_verifactu, verifactu_submit.py:306-392), NO un flujo de 'suministro a requerimiento de los registros conservados'; su unico caller es la ruta por factura invoices.py:534-537, sin remision masiva ni por periodo (grep de callers solo da esa ruta). (2) El envio esta deliberadamente inerte: la ruta cablea confirmed=False fijo (backend/app/api/v1/routes/invoices.py:537), el endpoint de produccion es None (verifactu_submit.py:38) y un guard bloquea el POST si VERIFACTU_SIF_NIF es placeholder 'B00000000' (verifactu_submit.py:264-270). Sin confirmed no hay POST (dry-run, verifactu_submit.py:251-256). (3) ConsultaLR.xsd esta descargado (backend/app/services/aeat/xsd/ConsultaLR.xsd) pero grep 'ConsultaLR' en backend/app da cero usos en .py -- no hay consulta/recuperacion implementada. Conclusion: el sistema hoy no puede suministrar nada real a la sede; solo hay infraestructura reutilizable (transporte mTLS, firma, validacion) construida para otro flujo y ademas apagada.
- Gap: Implementar el flujo de suministro por requerimiento: iterar los VerifactuRecord conservados del tenant (total o del periodo requerido) y remitirlos en lote por el transporte mTLS ya existente, con su envoltura SOAP/consulta. Configurar el endpoint de produccion (hoy None), retirar el confirmed=False cableado, configurar VERIFACTU_SIF_NIF real tras homologacion en preproduccion, y dar uso a ConsultaLR.xsd (hoy solo descargado). Nada de esto existe hoy: lo presente es maquinaria reutilizable de otro flujo (remision de alta) y esta inerte.

## integridad

### [PARCIAL] Art. 8.1 RD 1007/2023 — integridad, inalterabilidad, trazabilidad, conservacion, accesibilidad y legibilidad sin interpolaciones/modificaciones/omisiones no anotadas
- Evidencia: VERIFICADO. Inalterabilidad cuando hay registro: cadena SHA-256 por tenant con pg_advisory_xact_lock (verifactu_chain.py:227-234) y persistencia encadenada (:246-272); triggers PL/pgSQL rechazan UPDATE/DELETE SOLO en Postgres (0011_verifactu_chain.py:52-77), en SQLite la inmutabilidad es solo por codigo (0011:8-10, declarado en el propio docstring). Borrado bloqueado si hay VerifactuRecord (commands.py:336-338) o si la factura emitida no es draft (:344-348). Unica mutacion via HTTP es PATCH /invoices/{id}/status (invoices.py:292 -> update_status commands.py:264-287, cambia solo status, no importes). Maquina de estados sin reversa a draft (state_machine.py:51-57; existe reversa controlada paid->sent en :56, pero no a draft, coherente con la afirmacion). OMISIONES estructurales confirmadas: modo default no_remission NO genera registro alguno (verifactu_mode.py:18 DEFAULT_MODE, verifactu_chain.py:293-294 'if not should_remit: return None'); tenant sin NIF emite factura sin cadena con solo un warning (verifactu_chain.py:296-307); facturas importadas 'no encadena Verifactu' (import_bulk.py:127); backfill manual (backfill_verifactu.py).
- Gap: Generar SIEMPRE el registro de facturacion al emitir (la modalidad no-VERI*FACTU exige registro local firmado, no ausencia de registro); bloquear emision si falta NIF del emisor en vez de omitir la cadena; encadenar/backfillear automaticamente las importadas; en SQLite la inmutabilidad no esta garantizada a nivel BD.

### [PARCIAL] Art. 12 RD 1007/2023 — huella/hash en todos los registros de alta y anulacion y firma electronica (salvo excepcion VERI*FACTU art. 14 Orden)
- Evidencia: VERIFICADO. Huella de ALTA correcta: payload canonico oficial AEAT + SHA-256 hex mayusculas (verifactu_chain.py:50-81 build_payload_alta, :107-112 compute_huella) validado contra el vector oficial (test_verifactu_chain.py). ANULACION: existen build_payload_anulacion (verifactu_chain.py:84-104) y build_registro_anulacion_xml (registro_facturacion.py:313-347) pero SIN caller de aplicacion — grep de build_registro_anulacion_xml solo devuelve su def (:313) y un test (test_verifactu_registro_xml.py:112); el propio codigo declara 'la anulacion no se persiste como VerifactuRecord todavia' (:327). Las anulaciones se instrumentan como rectificativas R1 con alta propia encadenada (commands.py:250-254, tipo R1 en verifactu_chain.py:242). FIRMA ELECTRONICA de los registros: inexistente — xades_signer solo firma el XML de ENVIO y solo en modo voluntary+confirmed (verifactu_submit.py:279); en el modo default no_remission no se crea registro alguno, luego nada se firma.
- Gap: Persistir y encadenar registros de anulacion (hoy solo construccion XML sin caller ni persistencia); implementar firma electronica de los registros de alta/anulacion para la modalidad sin remision, o restringir el producto a VERI*FACTU exclusivo.

### [PARCIAL] Art. 6.b), 6.d), 6.e) Orden HAC/1177/2024 — comprobacion bajo demanda de huella individual, firma y cadena completa, de forma rapida/facil/intuitiva
- Evidencia: VERIFICADO y con matiz que DEBILITA la parte de huella individual. GET /api/v1/verify/{huella} existe y es publico (verify.py:27-64), pero su integrity_ok (verify.py:52) recomputa el SHA-256 del payload_canonico ALMACENADO y lo compara con la huella ALMACENADA de la MISMA fila: solo detecta divergencia interna payload<->huella, NO verifica el enlace de cadena (huella_anterior) ni firma alguna; un registro reescrito de forma internamente consistente devolveria integrity_ok=True. Cadena completa: verify_chain_integrity recorre y valida toda la cadena, detecta genesis ausente/multiple, ciclos y bifurcaciones (verifactu_chain.py:312-338 + order_verifactu_chain :120-166) — CORRECTO, PERO grep confirma que solo se invoca desde tests (test_verifactu_chain.py, test_idempotency.py, test_backfill_verifactu.py); no hay endpoint ni UI para verificar la cadena o un tramo bajo demanda. Verificacion de firma: inaplicable (los registros no se firman).
- Gap: Exponer verify_chain_integrity (total y por tramo) en un endpoint/pantalla invocable por el usuario; que la verificacion individual comprenda tambien el enlace de cadena, no solo la auto-consistencia payload/huella; anadir verificacion de firma cuando exista firma de registros.

### [PARCIAL] Art. 8.1 y 8.2 RD 1007/2023 — proceso tecnico fiable (registros fidedignos y completos desde su grabacion), trazabilidad encadenada del primero al ultimo con datos fechados, conservacion con descarga/volcado/archivo exportable legible; cualquier funcion que altere u oculte el rastro incumple
- Evidencia: VERIFICADO. Trazabilidad encadenada primero-a-ultimo por el ENLACE hash cuando hay registros (order_verifactu_chain/find_tail_huella verifactu_chain.py:120-186, inmune a timestamps identicos); registros fechados con FechaHoraHusoGenRegistro (:40-47,:254) y backfill distinguible (is_backfilled/backfilled_at, backfill_verifactu.py:191-204). PERO: (a) 'completo desde su grabacion' NO es certificable porque el modo default no graba registros (verifactu_chain.py:293-294); (b) confirmado que NO existe exportacion/volcado de los registros en formato electronico legible — generate_alta_xml solo tiene como caller de aplicacion verifactu_submit.py:245 (grep: def en registro_facturacion.py:399 + ese unico caller), sin endpoint de descarga de XML/cadena; el backup pg_dump es volcado de BD, no archivo legible de registros.
- Gap: Anadir procedimiento de descarga/volcado de los registros (XML oficiales o formato legible) a almacenamiento externo, y cerrar la incompletitud del modo default (grabar registro siempre).

### [PARCIAL] Art. 8 RD 1007/2023 y art. 201 bis.1.a)-d) LGT — prohibicion de doble uso: no permitir contabilidades distintas, ni omitir/alterar anotaciones de transacciones
- Evidencia: VERIFICADO. Contra alteracion (letra d): bien cubierto — triggers append-only en Postgres (0011:52-77), borrado bloqueado (commands.py:336-338,:344-348), sin edicion de importes de facturas emitidas (invoices.py solo expone PATCH /status en :292), anulacion solo via rectificativa encadenada (commands.py:250-254). PERO contra letra b) 'no reflejar total o parcialmente la anotacion': el sistema PERMITE emitir facturas sin ningun registro — modo no_remission por defecto (verifactu_mode.py:18, verifactu_chain.py:293-294), conmutable por tenant via PUT /verifactu/config (verifactu_config.py:47-68); set_mode solo escribe un log (verifactu_mode.py:57-62), NO registra evento de negocio del cambio voluntary->no_remission, que deja transacciones sin anotar; ademas la emision sin NIF omite la cadena silenciosamente (verifactu_chain.py:296-307). DIVERGENCIA doc/impl confirmada: el docstring de 0023 afirma que en no_remission 'el sistema genera la cadena huella y la conserva localmente' (0023_verifactu_config.py:6-8) pero should_remit devuelve True solo en voluntary (verifactu_mode.py:66-68) — el codigo real NO la genera.
- Gap: Encadenar huella en TODOS los modos (el modo debe decidir solo la remision, no la anotacion); bloquear emision sin NIF; registrar como evento auditable cualquier cambio de modo; corregir la divergencia del docstring 0023.

### [CUMPLE] Disp. adicional 1a Orden HAC/1177/2024 — pruebas solo en el Portal de pruebas externas (preproduccion), nunca contra produccion
- Evidencia: REFUTACION INTENTADA Y FALLIDA: la afirmacion resiste. Para VeriFactu, VERIFACTU_ENDPOINTS['produccion']=None (verifactu_submit.py:37) y HttpxVerifactuTransport.send lanza VerifactuSubmitError si el endpoint no esta configurado (:200-202): no hay ruta de codigo al servicio real. PreproduccionSubmitter fija environment='preproduccion' por defecto y get_submitter lo instancia SIN pasar environment (:238,:301-303), asi que ningun modo elige produccion. Sin confirmed=True no hay POST (dry-run, :251-256) y la unica ruta de aplicacion (send_to_verifactu) pasa confirmed=False (invoices.py:537); grep confirma que ningun caller de aplicacion pasa confirmed=True ni environment='produccion'. Guards adicionales abortan con NIF placeholder B00000000, sin certificado o con firma stub (:264-284). MATIZ (no refuta): el subsistema SEPARADO de presentacion de modelos 303/130 si define URLs de produccion y acepta environment='produccion' (sede_client.py:30,34,38; presentation_service.py:48), pero es otro servicio AEAT (presentacion de autoliquidaciones), fuera del alcance del SIF/VeriFactu que gobierna esta disposicion.

## declaracion

### [FALTA] El productor del SIF debe certificar mediante declaracion responsable, para cada sistema y cada version, que cumple el art. 29.2.j) LGT, el RRSIF y la Orden (art. 13.1-13.2 RD 1007/2023); cada productor/ampliacion emite su propia certificacion.
- Evidencia: No existe mecanismo de declaracion responsable. Grep 'DECLARACION RESPONSABLE|declaracion responsable' en todo el repo solo devuelve ficheros de analisis en tasks/ (gap-analysis-sif-verifactu.md:160,164,168), que la marcan como PENDIENTE, no como implementada. En backend/app/services/billing/ solo hay la identificacion del SIF para el XML del registro (registro_facturacion.py:63-100 dataclass SistemaInformatico), que NO es una declaracion responsable. Ni ruta, ni documento, ni almacenamiento (grep en backend/app/db devuelve 0 coincidencias de declaracion/productor).
- Gap: Implementar la emision/gestion de una declaracion responsable por sistema y version (documento firmado + su generacion), inexistente hoy.

### [FALTA] Contenido minimo (art. 15 Orden + 13.4 RD): identificar el SIF (nombre, version), tipologia, composicion, funcionalidades y caracteristicas de instalacion; datos identificativos y de localizacion del productor; fecha y lugar de firma; firmada y disponible (visible desde el propio SIF y para clientes/Administracion).
- Evidencia: No existe documento de declaracion, luego no hay contenido minimo. Solo hay datos parciales de identificacion en registro_facturacion.py:71-99 (nombre_razon, nif productor, nombre_sistema, id_sistema, version, numero_instalacion) usados para el XML del registro en _sistema_informatico (registro_facturacion.py:147-157). Faltan por completo: tipologia, composicion, funcionalidades, caracteristicas de instalacion, localizacion (direccion) del productor y fecha/lugar de firma. No se encontro firma ni publicacion de la declaracion.
- Gap: Crear la declaracion con TODO el contenido minimo (tipologia/composicion/funcionalidades/instalacion/localizacion productor/fecha-lugar firma), firmarla y publicarla.

### [FALTA] Certificacion por autodeclaracion del PRODUCTOR (no del usuario) de cumplimiento del art. 29.2.j LGT, el Reglamento del RD 1007/2023 y la Orden HAC/1177/2024, unico mecanismo de 'certificacion' previsto (sin homologacion previa AEAT) (art. 13.1).
- Evidencia: No hay manifestacion/certificacion alguna. Grep '29.2.j' en codigo devuelve solo tasks/gap-analysis-sif-verifactu.md:158 (analisis). El unico campo 'productor' real es el NIF del productor (registro_facturacion.py:95 nif=_s('VERIFACTU_SIF_NIF','B00000000'), placeholder), pero se usa para el XML, no para certificar cumplimiento. Ninguna estructura declara que el productor asuma el cumplimiento del 29.2.j/RD/Orden.
- Gap: Redactar y emitir, como productor, la autodeclaracion de cumplimiento del art. 29.2.j LGT + RD 1007/2023 + Orden HAC/1177/2024.

### [FALTA] La declaracion debe constar por escrito y visible EN EL PROPIO sistema en cada version, y ademas disponible para cliente/comercializador en la adquisicion; FAQ AEAT exige dos ubicaciones: interna (menu tipo Ayuda/Acerca de) y externa (consultable sin instalar el producto) (art. 13.2 RD + art. 15 Orden).
- Evidencia: No hay ubicacion interna ni externa. La pantalla de configuracion VeriFactu (frontend/src/app/(dashboard)/configuracion/verifactu/page.tsx:12,22-26) solo tiene pestanas 'verifactu' (modo), 'apoderamiento' y 'firma'; no hay pestana/opcion 'Acerca de'/'Ayuda' ni de declaracion. Grep 'Acerca de|declaracion' en frontend/src solo devuelve 'declaraciones' fiscales (es.json:1783,1811 PRESENTACION_DECLARACIONES), sin relacion. No existe pagina/endpoint publico externo con la declaracion.
- Gap: Anadir vista interna accesible (Acerca de/Ayuda) que muestre la declaracion de la version actual y una publicacion externa consultable sin instalar.

### [FALTA] El productor/comercializador debe guardar y conservar las declaraciones responsables de TODAS las versiones y entregarlas cuando lo solicite el cliente o la Administracion (art. 13.3 RD).
- Evidencia: No hay almacen de declaraciones ni historico por version. Grep 'declaracion|productor|SistemaInform' en backend/app/db (modelos + migraciones, incluidas 0011/0018/0023) devuelve 0 coincidencias: no existe tabla ni modelo de declaracion. No hay ningun VerifactuRecord ni entidad que persista declaraciones por version.
- Gap: Implementar conservacion versionada de las declaraciones responsables y un canal para entregarlas a cliente/AEAT bajo requerimiento.

### [FALTA] Contenido minimo segun Reglamento (art. 13.4 RD): datos que identifiquen el sistema y su tipologia, composicion y funcionalidades, caracteristicas de instalacion, datos identificativos y de localizacion del productor, y fecha y lugar de la firma.
- Evidencia: Mismo hallazgo que el contenido minimo del art. 15: no hay declaracion. Los unicos datos existentes (registro_facturacion.py:71-99) cubren solo identificacion basica del sistema y NIF del productor para el XML; faltan tipologia, composicion, funcionalidades, caracteristicas de instalacion, localizacion del productor y fecha/lugar de firma. No se hallo ningun artefacto que agregue estos campos.
- Gap: Modelar y rellenar los datos exigidos por el art. 13.4 dentro de una declaracion real.

### [FALTA] Formato normalizado (art. 15.1 Orden): la declaracion debe comenzar con el titulo literal 'DECLARACION RESPONSABLE DEL SISTEMA INFORMATICO DE FACTURACION' e incluir la informacion del art. 15.1 en el MISMO ORDEN, precediendo cada dato del texto descriptivo de su letra.
- Evidencia: No existe ningun documento con ese titulo. Grep del literal 'DECLARACION RESPONSABLE DEL SISTEMA' (case-insensitive) en todo el repo: 0 coincidencias en codigo/plantillas (solo apariciones sueltas del concepto en tasks/*.md de analisis). No hay plantilla PDF/HTML de la declaracion (glob de plantillas y revision de services/pdf_reports no muestra ninguna).
- Gap: Crear la plantilla normalizada con el titulo literal y el orden/etiquetado por letras del art. 15.1.

### [FALTA] Identificacion del sistema en la declaracion (art. 15.1.a-d): a) nombre del SIF; b) codigo IdSistemaInformatico (apartado 2.6 anexo, univoco por productor); c) identificador completo de la version; d) componentes hardware y software.
- Evidencia: No hay declaracion donde consten. Existe identificacion parcial para el XML del registro: nombre (registro_facturacion.py:96 nombre_sistema, tope 30 char en :151), IdSistemaInformatico (registro_facturacion.py:97 id_sistema[:2], tope 2 char por XSD segun :16 y :74), y version (:98). Pero d) 'componentes hardware y software' NO existe en ningun sitio, y a/b/c no se emiten como declaracion sino embebidos en cada RegistroAlta. No hay control de unicidad del codigo por productor mas alla del recorte a 2 caracteres.
- Gap: Trasladar a) b) c) a la declaracion y anadir d) descripcion de componentes HW/SW; garantizar univocidad documentada del IdSistemaInformatico.

### [FALTA] Identificacion del productor en la declaracion (art. 15.1.h-i-j): h) nombre/razon social de la entidad productora; i) NIF (o numero alternativo con tipo y pais); j) direccion postal completa de contacto.
- Evidencia: No hay declaracion. Datos parciales del productor solo para el XML: h) nombre_razon (registro_facturacion.py:94) e i) NIF (registro_facturacion.py:95, hoy placeholder 'B00000000'; bloqueo de envio si sigue placeholder en verifactu_submit.py:267-268). j) direccion postal del productor NO existe en ningun campo/settings (grep VERIFACTU_SIF_* en .env.example:63-68 solo tiene NIF, nombre_razon, nombre_sistema, id, version, num_instalacion; sin direccion). No hay soporte para NIF alternativo con tipo/pais.
- Gap: Anadir direccion postal del productor (y soporte NIF alternativo tipo/pais) y volcarlos a la declaracion.

### [FALTA] Manifestacion expresa de cumplimiento (art. 15.1.k-l): declarar que el SIF, en la version indicada, cumple art. 29.2.j LGT + Reglamento RD 1007/2023 + Orden HAC/1177/2024 + lo publicado en sede AEAT; con fecha completa (dia, mes, anio) y lugar (localidad y pais).
- Evidencia: No existe ninguna manifestacion de cumplimiento ni campos de fecha/lugar de firma de declaracion. Grep '29.2.j' en codigo: 0 (solo tasks/*.md). No hay estructura que recoja la frase de cumplimiento ni fecha/lugar de suscripcion de la declaracion (las unicas fechas del dominio son FechaHoraHusoGenRegistro del registro, sin relacion).
- Gap: Incluir la manifestacion literal de cumplimiento (k) y la fecha completa + lugar de firma (l).

### [FALTA] Si el SIF admite modalidad NO VERI*FACTU: la declaracion debe indicar los tipos de firma de los registros de facturacion y de evento (art. 15.1.g); ademas anexo recomendado con contacto/URLs/explicacion de cumplimiento (art. 15.2).
- Evidencia: No hay declaracion, luego no consta el tipo de firma ni anexo. El SIF se declara solo-VeriFactu por defecto (registro_facturacion.py:77 solo_verifactu='S', emitido como TipoUsoPosibleSoloVerifactu en :155); aun asi no existe ninguna indicacion documental de tipos de firma ni el anexo del art. 15.2. Grep no halla ningun anexo ni descripcion de firma para la declaracion.
- Gap: Si se soporta no-VeriFactu, declarar los tipos de firma (g) y anadir el anexo recomendado (15.2); en cualquier caso la declaracion base sigue faltando.

### [FALTA] La declaracion se refiere a UNA version concreta: cada version requiere su propia declaracion, el sistema debe mostrar la de su version actual, y con componentes de distintos productores se declara por componentes (art. 13.2-13.3 RD + art. 15 Orden).
- Evidencia: No hay declaracion por version ni visualizacion de la vigente. La version del SIF solo existe como cadena para el XML (registro_facturacion.py:98 version=_s('VERIFACTU_SIF_VERSION','1.0'); .env.example:67), sin vincularse a ninguna declaracion. No hay logica que muestre 'la declaracion de la version actual' ni soporte de declaracion por componentes/productores (grep en frontend y backend: 0 vistas/entidades de declaracion).
- Gap: Ligar cada declaracion a su version, mostrar la vigente en el SIF y soportar declaracion por componentes cuando intervengan varios productores.

## tpv-f2 _(restaurado del primer pase (mapeo); el verificado echo-fallo)_

### [FALTA] El campo «TipoFactura» del registro de alta debe soportar los valores de la lista L2 relevantes para TPV: F2 (factura simplificada), F3 (sustitutiva de simplificadas) y R5 (rectificativa de simplificadas).
- Evidencia: backend/app/services/billing/verifactu_chain.py:241-242 — el TipoFactura se decide con un ternario cerrado: tipo_factura = "R1" if invoice_type == "rectificativa" else "F1". Ningún camino de código puede producir F2, F3 ni R5. El builder XML es passthrough (backend/app/services/billing/registro_facturacion.py:290 emite p["TipoFactura"] tal cual) y el XSD oficial sí admite los valores (backend/app/services/aeat/xsd/SuministroInformacion.xsd:717 F2, :742 R5, :747 F3), pero eso es el esquema de la AEAT, no implementación propia. El módulo TPV existe pero declara que no emite factura (backend/app/db/models/pos.py:12-14: "No emite factura automáticamente — la integración con create_invoice y Verifactu queda como fase 2"); backend/app/api/v1/routes/pos.py no tiene ningún endpoint de facturación/ticket. Busqué "F2", "F3", "R5", "simplificad" en backend/app/**/*.py: cero asignaciones fuera del ternario F1/R1.
- Gap: Añadir soporte de TipoFactura F2 (venta TPV sin destinatario), F3 (sustitutiva) y R5 (rectificativa de simplificada) en build_payload_alta/verifactu_chain y en la API de creación de facturas; conectar el checkout del TPV con la emisión de factura simplificada F2.

### [FALTA] En facturas F2 el bloque de destinatarios puede omitirse y para tickets sin identificación del destinatario se usa el indicador «FacturaSinIdentifDestinatarioArt61d»='S'.
- Evidencia: grep de "SinIdentif" en backend/**/*.py: cero resultados; solo aparece en el XSD oficial (backend/app/services/aeat/xsd/SuministroInformacion.xsd:145, elemento FacturaSinIdentifDestinatarioArt61d). El builder del RegistroAlta (backend/app/services/billing/registro_facturacion.py:256-310) no emite jamás el indicador. Nota agravante: tampoco emite NUNCA el bloque Destinatarios (grep "Destinatario" en services/billing/*.py: cero resultados) — es decir, hoy los F1 salen sin destinatario (lo que la AEAT rechazaría para F1) y los F2 con indicador no existen. No hay modelado de "ticket sin cliente" en Invoice.
- Gap: Implementar la rama F2: omitir Destinatarios legítimamente y emitir FacturaSinIdentifDestinatarioArt61d='S' cuando la venta es sin identificación (art. 6.1.d RD 1619/2012); simétricamente, añadir el bloque Destinatarios obligatorio para F1.

### [FALTA] Las facturas F3 emitidas en sustitución de simplificadas deben identificar las facturas simplificadas sustituidas (bloque «FacturasSustituidas» del RegistroAlta).
- Evidencia: grep de "FacturasSustituidas|Sustituid" en backend/**/*.py: cero resultados; el elemento solo existe en el XSD oficial (backend/app/services/aeat/xsd/SuministroInformacion.xsd:131). El único mecanismo análogo implementado es FacturasRectificadas para R1-R5 (backend/app/services/billing/registro_facturacion.py:293-300), que se activa si TipoFactura empieza por "R" — nunca cubre F3, y además F3 no puede generarse (verifactu_chain.py:241-242 solo F1/R1). No existe flujo de "sustitución de simplificadas" en rutas ni servicios (routes/invoices.py solo tiene /rectificativa, línea 380).
- Gap: Crear el flujo F3 (factura completa en sustitución de simplificadas ya declaradas) con el bloque FacturasSustituidas poblado con IDFactura de cada simplificada sustituida.

### [PARCIAL] QR y, en modo VERI*FACTU, leyenda obligatorios también en facturas simplificadas (tickets de TPV) — art. 6.5 RD 1619/2012 y art. 20 Orden HAC/1177/2024.
- Evidencia: Existe QR en el PDF de facturas normales: backend/app/services/pdf/_invoice_sections.py:328-390 (_verifactu_qr_block) insertado en backend/app/services/pdf/invoices.py:176-177. Pero: (1) no hay leyenda «Factura verificable en la sede electrónica de la AEAT» / «VERI*FACTU» — grep de "verificable|leyenda|VERI\*FACTU" en billing/commands.py, billing/queries.py y pdf/_invoice_sections.py: cero; el texto real es "Verifactu — Escanea para verificar" (_invoice_sections.py:374); (2) la URL del QR es autoalojada, {FRONTEND_URL}/api/v1/verify/{huella} (backend/app/services/billing/queries.py:145), no el servicio de cotejo de la AEAT; (3) los tickets de TPV no existen como documento: pos.py:12-14 declara que la sesión TPV no emite factura y routes/pos.py no tiene endpoint de ticket/PDF, luego ninguna simplificada lleva QR porque ninguna simplificada se emite; (4) el QR es opcional por diseño (_invoice_sections.py:333: "la factura sigue siendo válida sin QR").
- Gap: Emitir factura simplificada desde el checkout TPV con QR conforme al art. 20-21 de la Orden (URL de cotejo AEAT con sus parámetros) y la leyenda «VERI*FACTU» / «Factura verificable en la sede electrónica de la AEAT» cuando se opera en modo VERI*FACTU; hacer el QR obligatorio, no best-effort.

### [FALTA] Los envíos de registros F2 deben pasar las validaciones específicas del documento de validaciones y errores de la AEAT (p. ej. límite de importe total para F2, bloques admitidos por tipo de factura) antes de enviar.
- Evidencia: La única validación pre-envío es estructural contra el XSD: backend/app/services/billing/verifactu_submit.py:246-248 llama a validate_verifactu_xml (backend/app/services/billing/registro_facturacion.py:353-396), que valida SuministroLR.xsd con lxml. El documento de validaciones de la AEAT (reglas de negocio: límite de ImporteTotal para F2, destinatario obligatorio según TipoFactura, bloques admitidos por tipo) no está implementado en ningún sitio: grep de "3000|limite.*importe|MAX_.*IMPORTE|validacion" en verifactu_submit.py y services/aeat/*.py no devuelve ninguna regla de importe; no existe módulo de validaciones de negocio VeriFactu. Además F2 ni siquiera puede generarse (verifactu_chain.py:241-242), por lo que la validación específica de F2 es doblemente inexistente.
- Gap: Implementar una capa de validaciones de negocio pre-envío basada en el documento oficial de validaciones y errores VERI*FACTU (límite de importe total F2, coherencia TipoFactura↔bloques Destinatarios/FacturasSustituidas/FacturasRectificadas, etc.) que se ejecute antes del POST a la sede, además del XSD.

